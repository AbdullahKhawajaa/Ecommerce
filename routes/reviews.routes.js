const express = require("express");
const router = express.Router();

const review_controller = require("../controllers/rating.controller");

//router.get("/reviews",review_controller.addReview); 


module.exports = router;